services:
  robomaster-sim:
    build: .
    stdin_open: true
    tty: true
    environment:
      - SIM=${SIM}
      - WORLD=${WORLD}
      - ROBOMASTER_IP=${ROBOMASTER_IP}
    volumes:
      - ./ros2_ws:/root/ros2_ws